TODO

adding shadows

When in production we will change the bootstrap and jQuery to CDN links instead of local

We also have to remove comments at last.

apply the onHover for enlargment scale up the image

apply the shadows to the search box image and text area over it

text imported dynamically by php srcipt


